Directory Size Calculator

A simple Python script that calculates the total size of files in a given directory.

Usage

Run the script:

python directory_size.py

Enter the directory path when prompted:

Enter the Directory path: /path/to/directory

The script displays the size of each file and the total directory size in bytes.

Requirements

* Python 3.x
* No external dependencies
